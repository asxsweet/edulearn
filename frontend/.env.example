# Vercel: project Root Directory = "frontend" (this folder). Build command: npm run build. Output: dist.
# Public API base URL — your Render (or other) API origin, no trailing slash. Required in production.
# VITE_API_URL=https://your-api.onrender.com
